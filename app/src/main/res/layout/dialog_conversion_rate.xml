<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Currency Conversion"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="To keep your history accurate, please enter the current exchange rate:"
        android:layout_marginBottom="16dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/tvConversionMessage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="1 USD = ? EUR"
            android:textStyle="bold"
            android:textSize="16sp"/>

        <ImageButton
            android:id="@+id/btnSwap"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@color/purple_200"
            android:contentDescription="Swap Direction"
            android:rotation="90"
            android:src="@android:drawable/stat_notify_sync_noanim" />
            <!-- Using rotated menu icon as pseudo-arrow for now until vector asset added -->
            <!-- Or just use simple Text "Swap" if icon is issue -->
    </LinearLayout>

    <EditText
        android:id="@+id/etExchangeRate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:hint="Exchange Rate"
        android:background="@android:drawable/edit_text"
        android:minHeight="48dp"/>

</LinearLayout>
